# More info at https://github.com/guard/guard#readme

guard 'sass' do
  watch(%r{sass/.+\.(scss|sass)})
end

guard 'livereload' do
  watch(%r{.+\.(html|htm)})
  watch(%r{css/.+\.(css)})
  watch(%r{img/.+\.(png|jpeg|jpg)})
  watch(%r{js/.+\.(js)})
end
